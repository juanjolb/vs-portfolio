import { Routes } from '@angular/router';
import { SkillsComponent } from '../../pages/side-panel/skills/skills.component';

export const routes: Routes = [
    {
        path: 'skills',
        component: SkillsComponent
    },
];
